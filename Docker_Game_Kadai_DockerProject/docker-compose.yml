version: '3.8'

services:
  game-server:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./json_data:/app/json_data
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
